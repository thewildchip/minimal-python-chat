# Minimal Python Chat

Minimal Python Chat provides users a bare-bone script that two hosts can run to chat with each other.

The IPv4 of both devices is required to run it on both sides.

The connections default to the port 3222.
