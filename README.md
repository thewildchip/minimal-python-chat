# Minimal Python Chat

Minimal Python Chat provides users a bare-bone script that two hosts can run to chat with each other.
 
The IPv4 of both devices is required to run it on both sides. It is also required to be in the same network. The connections default to the port 3222.

The listen Function usually needs to be pkilled extra even after ^c although that can differ between different OS.

## License
MIT
